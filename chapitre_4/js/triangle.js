// on initialise les lignes à une ligne VIDE (sans #)
var ligne = "";

// POUR le variable i = 1 jusqu'à i = 7, on rajoute une ligne et "#" à la ligne"
for (var i = 0; i <= 7, i++){
	ligne = ligne + "#";
}

// on affiche en console les lignes
console.log(ligne);